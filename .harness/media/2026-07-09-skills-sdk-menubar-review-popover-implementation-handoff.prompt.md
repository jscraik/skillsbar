# 2026-07-09 Skills SDK Menubar Review Popover Implementation Handoff Prompt

Generated image:

- Repository media path: `.harness/media/2026-07-09-skills-sdk-menubar-review-popover-implementation-handoff.png`
- Purpose: review-only implementation-handoff visual for the approved Skills SDK macOS MenuBarExtra slice.
- Closure evidence status: generated visual reference; not live app proof.

Prompt:

Use case: ui-mockup
Asset type: final macOS MenuBarExtra implementation-handoff mockup
Primary request: precisely edit the persisted final-polish Skills SDK menubar mockup while preserving its long portrait composition, selected menubar icon, native anchored notch, dark translucent graphite material, status-first hierarchy, score hex, expanded local review trigger, secondary Tessl Registry row, mixed-status bridge, and neutral copy action.
Input images: final-polish mockup is the edit target.
Text: preserve all existing labels and values except make the command preview show `cd '/Users/jamiecraik/dev/agent-skills' && ./bin/ask sdk security risk-modes 'Skills/agent-ops/improve-agent-native/SKILL.md' --preview --json --robot`, wrapped cleanly across three stable monospace lines.
Changes: remove the top-right details/document-search control entirely and rebalance the header without replacing it; retain a restrained Skills SDK header mark, monochrome menubar mark, and distinct Skills SDK/Tessl row marks as optical-size treatments; keep the popover visually equivalent to a `404 x 720 pt` macOS surface at 2x scale.
Constraints: the copy control remains the only focusable action shown; retain the green check plus amber-dot mixed-status bridge glyph; keep amber diagnostic-only and the action neutral; no chevrons, accordion cues, Terminal-launch affordance, registry-open affordance, recovery action, helper footer, marketing copy, blue syntax highlighting, decorative glow, or extra controls.
Invariants: do not change Needs review, 3 risks need inspection, score 78, jscraik/improve-agent-native, Private, Quality 100%, Impact 71/71, Security 3 risks, 1 critical / 2 high, Tessl v0.2.0 score 66, registry Quality 100%, Impact 63%, Security Passed, Registry clean. Local source has findings., or 68 registry eval scenarios.

Generator mode: built-in ImageGen edit.

Framing correction:

- Extended the portrait composition below the action card so the full rounded popover bottom and a visible safe-area margin are present.
- Preserved the implementation-handoff content and command contract without adding a footer or control.
