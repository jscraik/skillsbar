import CoreGraphics

enum MenuBarTemplateMetrics {
    // Keep the MenuBarExtra inside a laptop-height visible frame. The full
    // evidence stack remains available through DashboardView's internal scroll.
    static let width: CGFloat = 404
    static let height: CGFloat = 720
}
